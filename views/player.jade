div.flybutton(ng-mousedown='fly();$event.preventDefault();' ng-style='{background:color}')
    div.header
        div.text(ng-bind="me.handle + ' '+ me.score + '/'+ me.highscore + ' rank: ' + me.rank + '/'+$game.players.all.length")
    img.player(src="../img/button.png" ng-visible="me.playing" ng-mousedown='fly();$event.preventDefault();' ng-click="$event.preventDefault()")